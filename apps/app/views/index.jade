import:(src="./home")
import:(src="./root")

Title:
  | {{_page.title}}

Body:
  view(is="header")
  view(is="{{$render.ns}}")
  view(is="footer", year="2014")

header:
  h1 Header

footer:
  p Footer {{@year}}